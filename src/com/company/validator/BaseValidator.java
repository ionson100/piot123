package com.company.validator;

/**
 * Базовый класс для валидации кодов маркировки.
 * Содержит общие методы, которые могут быть переопределены или использованы в наследниках.
 * На данный момент реализации являются заглушками — должны быть дополнены по бизнес-логике.
 */
class BaseValidator {

    /**
     * Возвращает идентификатор группы товара по коду маркировки.
     * Группировка может использоваться для применения правил, скидок, ограничений и т.д.
     *
     * @param km Полный код маркировки (КИЗ)
     * @return идентификатор группы товаров; 0 — если группа не найдена
     */
    public int getGroupId(String km) {
        // TODO: Логика определения группы товара (например, по GTIN или другим атрибутам)
        return 3;
    }

    /**
     * Возвращает объём (емкость) блока (групповой упаковки) товара — например, количество единиц,
     *
     * @param km Полный код маркировки (КИЗ)
     * @return емкость блока; например, 10 штук
     */
    public int getCapacityBlock(String km) {
        // TODO: Определить логику расчета количества элементов внутри одной групповой упаковки
        return 10;
    }
}